<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.poly.GuitarAssistant">
    <uses-permission
        android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_guitar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_guitar_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Lecture.tong1" />
        <activity android:name=".Instrument.Instrument" />
        <activity android:name=".GuitarChord.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GuitarChord.MainActivity">
        </activity>
    </application>

</manifest>